export const selectSidebarIsOpen = (state) => state.sidebar.isOpen;

export const selectSidebarActiveItem = (state) =>
  state.sidebar.activeItem;

export const selectSidebarState = (state) => state.sidebar;