/**
 * @module Shape
 * @description Neo Brutalism = no rounded corners.
 * Border-radius 0 di semua tempat, border-width tebal.
 */
const Shape = {
  borderRadiusSmall: 0,
  borderRadiusMedium: 0,
  borderRadiusLarge: 0,

  buttonShape: { borderRadius: 0 },
  inputShape: { borderRadius: 0, borderWidth: 2 },
  cardShape: { borderRadius: 0 },
};

export default Shape;